# Sandbox — isolated space for AI-generated code experiments.
# Files here are written by SynthesisAgent and tested by TesterAgent via subprocess.
# Nothing here is imported into the main brain process directly —
# hot-loading is handled by Orchestrator._hot_load_module() after TesterAgent approval.
